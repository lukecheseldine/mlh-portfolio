lukeButton = document.getElementById('luke-button');
lukeButton.addEventListener('click', () => {
    document.getElementById('luke').scrollIntoView({
        'behavior': 'smooth',
        'block': 'center',
        'inline': 'center'
    });
})

mustafaButton = document.getElementById('mustafa-button');
mustafaButton.addEventListener('click', () => {
    document.getElementById('mustafa').scrollIntoView({
        'behavior': 'smooth',
        'block': 'center',
        'inline': 'center'
    });
})

samButton = document.getElementById('sam-button');
samButton.addEventListener('click', () => {
    document.getElementById('sam').scrollIntoView({
        'behavior': 'smooth',
        'block': 'center',
        'inline': 'center'
    });
})